import React, { Component } from 'react'
import { Route } from 'react-router-dom'
import './expenses.scss'


const Expense = ({match}) => {

    return (
        <div className="shop">
            {/* <Route exact path={`${match.path}`} component={} />
            <Route exact path={`${match.path}/:category`} component={} /> */}
        </div>
    )
    
}


export default Expense