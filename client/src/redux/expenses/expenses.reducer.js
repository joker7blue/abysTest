
const INITIAL_STATE = {
    expenses: []
};

const expensesReducer = (state = INITIAL_STATE, action) => {

    switch (action.type) {

        default:
            return state;
    }
}

export default expensesReducer;